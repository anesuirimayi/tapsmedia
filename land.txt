<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Taps Media | Premium Photography</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Montserrat:wght@400;500;600&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Montserrat', sans-serif;
            color: #fff;
            overflow-x: hidden;
            background: #000;
        }

        /* Header with transparent background */
        header {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            padding: 25px 40px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            z-index: 1000;
            transition: 0.4s;
        }

        header.scrolled {
            background: rgba(0, 0, 0, 0.7);
            backdrop-filter: blur(10px);
            padding: 15px 40px;
            box-shadow: 0 5px 20px rgba(0, 0, 0, 0.1);
        }

        .logo {
            font-family: 'Bebas Neue', cursive;
            font-size: 32px;
            letter-spacing: 3px;
            text-transform: uppercase;
            color: #fff;
            text-decoration: none;
        }

        .nav-links {
            display: flex;
            list-style: none;
        }

        .nav-links li {
            margin: 0 20px;
        }

        .nav-links a {
            font-family: 'Bebas Neue', cursive;
            color: #fff;
            text-decoration: none;
            text-transform: uppercase;
            letter-spacing: 2px;
            font-size: 20px;
            position: relative;
            padding: 5px 0;
            transition: 0.3s;
        }

        .nav-links a:after {
            content: '';
            position: absolute;
            width: 0;
            height: 2px;
            bottom: 0;
            left: 0;
            background-color: #fff;
            transition: width 0.3s ease;
        }

        .nav-links a:hover:after {
            width: 100%;
        }

        .hamburger {
            display: none;
            cursor: pointer;
        }

        .hamburger div {
            width: 25px;
            height: 2px;
            background-color: #fff;
            margin: 6px 0;
            transition: 0.3s;
        }

        /* Hero Section */
        .hero {
            height: 100vh;
            background: linear-gradient(rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.3)), url('https://images.unsplash.com/photo-1492684223066-81342ee5ff30?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1740&q=80') no-repeat center center/cover;
            display: flex;
            align-items: center;
            padding: 0 10%;
            position: relative;
        }

        .hero-content {
            max-width: 800px;
        }

        .hero h1 {
            font-family: 'Bebas Neue', cursive;
            font-size: 5.5rem;
            letter-spacing: 4px;
            line-height: 1.1;
            text-transform: uppercase;
            margin-bottom: 25px;
            text-shadow: 2px 2px 10px rgba(0, 0, 0, 0.3);
        }

        .hero p {
            font-size: 1.2rem;
            max-width: 600px;
            margin-bottom: 40px;
            line-height: 1.6;
            font-weight: 400;
        }

        .cta-buttons {
            display: flex;
            gap: 20px;
        }

        .btn {
            padding: 15px 35px;
            border-radius: 50px;
            text-transform: uppercase;
            font-weight: 600;
            letter-spacing: 1.5px;
            font-size: 14px;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .btn-explore {
            background: transparent;
            border: 2px solid #fff;
            color: #fff;
        }

        .btn-explore:hover {
            background: #fff;
            color: #000;
        }

        .btn-book {
            background: #e2a355;
            border: 2px solid #e2a355;
            color: #000;
        }

        .btn-book:hover {
            background: #d38c3a;
            border-color: #d38c3a;
        }

        /* Mobile Navigation */
        @media screen and (max-width: 968px) {
            .nav-links {
                position: fixed;
                top: 0;
                right: 0;
                height: 100vh;
                width: 250px;
                background: rgba(0, 0, 0, 0.9);
                backdrop-filter: blur(10px);
                flex-direction: column;
                align-items: center;
                justify-content: center;
                clip-path: circle(0px at top right);
                transition: all 0.8s ease-out;
                z-index: 999;
            }

            .nav-links.open {
                clip-path: circle(1000px at top right);
            }

            .nav-links li {
                margin: 20px 0;
            }

            .nav-links a {
                font-size: 24px;
            }

            .hamburger {
                display: block;
                z-index: 1000;
            }

            .hamburger.active div:nth-child(1) {
                transform: rotate(45deg) translate(5px, 6px);
            }

            .hamburger.active div:nth-child(2) {
                opacity: 0;
            }

            .hamburger.active div:nth-child(3) {
                transform: rotate(-45deg) translate(5px, -6px);
            }

            .hero h1 {
                font-size: 4rem;
            }
        }

        @media screen and (max-width: 576px) {
            header {
                padding: 20px;
            }
            
            header.scrolled {
                padding: 12px 20px;
            }
            
            .logo {
                font-size: 26px;
            }
            
            .hero {
                padding: 0 5%;
            }
            
            .hero h1 {
                font-size: 3rem;
            }
            
            .hero p {
                font-size: 1rem;
            }
            
            .cta-buttons {
                flex-direction: column;
                gap: 15px;
            }
            
            .btn {
                padding: 12px 30px;
                text-align: center;
            }
        }
    </style>
</head>
<body>
    <!-- Header with Navigation -->
    <header id="header">
        <a href="#" class="logo">TAPS MEDIA</a>
        
        <ul class="nav-links">
            <li><a href="#">HOME</a></li>
            <li><a href="#">GALLERY</a></li>
            <li><a href="#">WEDDINGS</a></li>
            <li><a href="#">ABOUT</a></li>
        </ul>
        
        <div class="hamburger">
            <div class="line1"></div>
            <div class="line2"></div>
            <div class="line3"></div>
        </div>
    </header>

    <!-- Hero Section -->
    <section class="hero">
        <div class="hero-content">
            <h1>We capture your best moments</h1>
            <p>We make your memories unforgettable by capturing your best moments in best quality.</p>
            <div class="cta-buttons">
                <a href="#gallery" class="btn btn-explore">Explore Our Work</a>
                <a href="https://wa.me/1234567890" target="_blank" class="btn btn-book">Book a Session</a>
            </div>
        </div>
    </section>

    <script>
        // Toggle mobile navigation
        const hamburger = document.querySelector('.hamburger');
        const navLinks = document.querySelector('.nav-links');
        const body = document.querySelector('body');
        
        hamburger.addEventListener('click', () => {
            hamburger.classList.toggle('active');
            navLinks.classList.toggle('open');
            body.classList.toggle('no-scroll');
        });
        
        // Header scroll effect
        const header = document.getElementById('header');
        
        window.addEventListener('scroll', () => {
            if (window.scrollY > 100) {
                header.classList.add('scrolled');
            } else {
                header.classList.remove('scrolled');
            }
        });
        
        // Close mobile menu when clicking on a link
        document.querySelectorAll('.nav-links a').forEach(link => {
            link.addEventListener('click', () => {
                hamburger.classList.remove('active');
                navLinks.classList.remove('open');
                body.classList.remove('no-scroll');
            });
        });
    </script>
</body>
</html>